[project]
name = "my-project"
version = "0.1.0"
description = "Example project using uv"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "requests>=2.31.0",
    "flask>=3.0.0"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "black",
    "ruff"
]